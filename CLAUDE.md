# 15kg減量＆ボディメイクプロジェクト - モバイルダッシュボード

## 1. プロジェクト概要 & バディ体制
- **ユーザー**: 薫之介（タダノスケ）さん
- **目標**: 半年で15kg減量（90.0kg ➔ 75.0kg、Vシェイプ構築）
- **バディ体制**:
  1. **Gemini Spark (メインAIコーチ/秘書)**: 日常対話、食事写真解析、全データ管理、朝食後の当日AI計画作成、毎夜23:30の振り返り・カロリー収支・前倒し日数配信。
  2. **Claude Code (エンジニア/データ解析)**: 本Webアプリの開発・改修・デプロイ。「筋トレMEMO」UI再現、計画参照、週間分析の構築。

## 2. 共通データベース & データ連携構造
- **共通スプレッドシート**: 『ボディメイク＆減量プロジェクト_総合管理シート』 (`1wJefKcr0S2hPcI9s7e1c89kWabnYpgQa0eg315pxtlE`)
- **指示書ドキュメント**: 『15kg減量＆ボディメイクプロジェクト_指示書』 (`1K_0vo2KIpjdZDmvC3bhLQREIQekqTSSR-gOBOd17jnY`)
  - URL: https://docs.google.com/document/d/1K_0vo2KIpjdZDmvC3bhLQREIQekqTSSR-gOBOd17jnY/edit?usp=sharing
  - Gemini Spark が毎回参照する最上位指示書。Geminiの日常コーチングや運用前提に関わる変更は、このDocsにも反映する。
- **タブ構成**:
  - `ボディメイク＆減量プロジェクト_総合管理シート`: A〜K列（日付/体重/摂取cal/P/F/C/消費cal/歩数/筋トレ/その他運動/メモ・コンディション）
  - `進捗＆予測ダッシュボード`: Gemini Sparkが朝食後に「AI次回計画メニュー (YYYY/MM/DD 部位 場所)」見出しで当日メニューを記入
  - `CLAUDE_MD_MASTER`: A1セルに最新のCLAUDE.md全文（`npm run sync-claude` でローカルへ取得）
  - `アプリ仕様_Claude→Gemini`: Claude Codeがアプリの仕様・変更点をGemini Sparkへ申し送るタブ（`npm run sync-gemini` で自動更新）
  - `AI運用ルール_必読`: Claude Code / Codex / Gemini Spark が作業前に必ず読む三者運用ルール
  - `AI変更履歴`: どのAIが、いつ、何を、なぜ変更したかを残す追記専用ログ
  - `AI会話議事録`: ユーザーとの会話で決まった要件・運用・役割変更の要約ログ
- **主要データ連携項目**:
  - `AI次回計画メニュー`: Gemini Sparkが朝食後に書き込んだ当日の予定メニュー（部位・種目・目標重量・目標回数・セット数・レスト時間）をアプリ側で自動読み込み表示。
  - `週間カロリー収支`: 過去7日間の累計カロリー収支。前倒し/遅れ日数のような機械的な直線予測はアプリ側では表示しない。
  - `メモ・コンディション(K列)`: Geminiが朝報告をもとに「睡眠: / 筋肉痛: / 今日: / 朝食:」を書き込む。アプリはK列を上書きせず、キー名と `:`/`：` を正規表現で解析する。

### 【絶対禁止（NG）種目】
- **ダンベルフライ**: ユーザーの明確な好みに基づき、アプリ初期種目・提案から完全除外。

### 【店舗別機材ルール】
- **赤坂店 (LifeFit)**: レッグカールなし、アシストチンニングあり
- **牛久/自宅近く店 (LifeFit)**: レッグカールあり、アシストチンニングなし

## 3. UI/UX ＆ 機能要件
### ① ダッシュボード画面
- **科学KPIカード**: 「ノイズ除去した実質体重」「今週の減量スピード」「実測消費代謝量 (TDEE)」「計算上の純脂肪カット量」を日本語ラベルで表示。
- **体重推移グラフ**: 生の測定体重は点、`Y_t = 0.133 * X_t + 0.867 * Y_{t-1}` の実質体重は線、Phase 1→2→3の未来目標ガイドラインは点線で重畳表示。機械的な直線予測や「計画よりX日遅れ/前倒し」は表示しない。
- **3 Phaseロードマップ**: Phase 1(90→84kg, 2000kcal P155/F44/C245)、Phase 2(84→79kg, 1950kcal P165/F43/C225, リフィード)、Phase 3(79→75kg, 1900kcal P175/F46/C195, ダイエットブレイク)を現在位置つきで表示。
- **進捗表示**: 正本スタート体重 90.0kg を開始基準として、現在値・減少量・目標までの残量を表示。記録内の最大体重は開始基準にしない。
- **本日のAI計画 (PLAN) カード**: Geminiが朝食後に設定した本日の目標メニューを表示し、筋トレタブで計画を適用できる。

### ② 独立した筋トレ記録画面（「筋トレMEMO」アプリUIの完全再現）
- **専用独立画面/タブ**: 記録フォーム内の狭いUIではなく、独立したフルサイズ画面（「筋トレ」タブまたは専用画面）。
- **赤基調のビジュアル**: 赤ヘッダー・赤カード・大きな文字・大きなボタン。
- **カレンダー & 月間アーカイブ & 合計負荷量 (Total Volume / トン数)** の表示。
- **AI計画のワンタップ引用**: 「AI計画を読み込む」ボタンで、目標重量・回数をセット入力欄にプリセット。
- **レスト (インターバル) タイマー**: セット完了時にワンタップでカウントダウン（60秒/90秒/120秒、音・バイブ通知）開始。
- **1RM自動計算表示**: `重量 × (1 + 回数/30)` のリアルタイム表示。
- **種目の自作追加**: プリセットにない種目をユーザー自身が追加・削除できる。
- **過去のトレーニング履歴**: アプリの保存記録とスプレッドシート I列 の内容を日付でマージして表示。
  展開すると全セットが見え、そのまま入力欄へ読み込んで編集できる。合計負荷量・記録日数も
  このマージ結果から算出する（＝シートにしか無い過去分も反映される）。
- **筋トレ実績の入力元はアプリのみ**。I列はアプリ専用で Gemini は書かない
  （Geminiの担当は「朝の当日計画」であり「当日の実績」ではない）。
- I列の書式: `[胸]ベンチプレス: 60kg×10回, 65kg×8回 / [腕]アームカール: 12kg×12回 ｜ 計3セット/負荷量1996kg`
  末尾の「回」は必須（旧書式のセット番号前置 `1.60kg×10` と機械的に区別するため）。
  書き出しは `formatWorkoutForSheet`、読み戻しは `parseSheetWorkout` が対で担当する。

### ②-b 履歴タブ（廃止）
2026-08-10に削除。筋トレ履歴は「筋トレ」タブへ、体重・カロリー推移はホームへ統合済み。
タブは ホーム / 今日 / 筋トレ / 設定 の4つ。

### ③ 設定画面（マルチユーザー対応）
- 各ユーザーが**自分の**スプレッドシート（GASウェブアプリURL）を登録する。サーバー側に共有の接続先は持たない。
- 未連携でもアプリ内（Firestore）への記録は可能。連携するとカロリー分析とAI計画が有効になる。

## 4. 主要開発・デプロイコマンド
- **ビルド・テスト**: `npm run build` && `npx tsc --noEmit`
- **Git & Vercel自動デプロイ**: `git add . && git commit -m "feat: [内容]" && git push origin main`
- **GASデプロイ**: `npm run deploy-gas`（clasp push + deploy）
- **CLAUDE.md取得**: `npm run sync-claude`（CLAUDE_MD_MASTER の A1 を取得）
- **Gemini申し送り更新**: `npm run sync-gemini`（`docs/GEMINI_BRIEF.md` → 『アプリ仕様_Claude→Gemini』タブ）

## 5. 【必須】変更のたびに毎回行うこと
アプリの仕様・機能・データ書式を変更したら、デプロイ後に**必ず**以下を実行する。
Gemini Sparkはこのタブを見てアプリの現状を把握するため、更新を怠ると連携がずれる。

1. `docs/GEMINI_BRIEF.md` を今回の変更内容に合わせて更新する
   - 機能を足した/消した → §7 機能一覧
   - データの書式や列の意味を変えた → §3 / §4（**最重要**。ここがずれるとAI計画が読めなくなる）
   - Geminiに依頼したいことが変わった → §9
2. `npm run sync-gemini` を実行してスプレッドシートへ転写する
3. 実行結果（更新文字数）を確認し、ユーザーに何を申し送ったか報告する

## 6. 【必須】3AI運用・議事録・変更履歴プロトコル
Claude Code / Codex / Gemini Spark の全員がスプレッドシートを閲覧・編集できるため、
勝手な変更や古い前提での誤実装を防ぐ。作業前後に必ず以下を守ること。

### 作業前に必ず参照する順序
1. `AI運用ルール_必読`
2. `AI変更履歴` の最新20件
3. `AI会話議事録` の最新20件
4. `アプリ仕様_Claude→Gemini`
5. Google Docs `15kg減量＆ボディメイクプロジェクト_指示書`
6. `CLAUDE_MD_MASTER` / ローカル `CLAUDE.md` / `AGENTS.md`

### 変更後に必ず記録すること
- スプレッドシート、GAS、アプリコード、仕様文書、データ書式、AI役割を変更したら、
  `AI変更履歴` に日時・担当AI・使用ツール・対象・変更種別・要約・理由・ユーザー確認有無・関連commit/範囲を追記する。
- ユーザーとの会話で要件、運用、データ書式、AIの役割、食事/運動方針が決まったら、
  `AI会話議事録` に会話要約・決定事項・次アクション・影響するタブ/文書を追記する。
- Geminiの認識に影響する変更は `docs/GEMINI_BRIEF.md` を更新し、`npm run sync-gemini` を実行する。
- Geminiの日常コーチングや毎回参照する前提に影響する変更は、Google Docs 指示書にも反映する。直接編集できない場合は `AI会話議事録` / `AI変更履歴` に「Docs更新待ち」と残し、ユーザーに依頼する。
- 所有者が曖昧なセルや既存データを消す変更は、推測で進めずユーザーに確認する。

### 既知の制約
- **Firestore**: プロジェクト `bodymake-37d49`。2026-08-10に有効化済み（asia-northeast1）。
  `src/lib/firestore.ts` は localStorage を正とし、全呼び出しに6秒の上限を設けてある。
  Firestoreが落ちても固まらず動作するので、この設計は崩さないこと。
- **サーバー側でFirestoreを呼ばない**: `src/app/api/**` から client SDK を呼ぶと、
  サーバーには認証情報が無いためルールに弾かれ、しかも失敗するまで数十秒ハングする。
  `/api/sheets` と `/api/log` はGASへの転送だけを行い、Firestoreの読み書きは
  必ずブラウザ側（コンポーネント）で行うこと。
- **Firestoreルールの期限**: テストモードで作成した場合、ルールに
  `allow read, write: if request.time < timestamp.date(...)` が入っており、
  **その日を過ぎると全機能が権限エラーで停止する**。必ず下記の恒久ルールに置き換える。
  ```
  match /users/{uid}/{doc=**} {
    allow read, write: if request.auth != null && request.auth.uid == uid;
  }
  ```
  `request.auth.uid == uid` が無いと、ログインさえすれば他人の記録を読めてしまう。
- **GASの追加権限**: ウェブアプリは実行時に必要と判定されたスコープしか要求しない。
  DriveAppなど新しいAPIを使うと承認が必要になるため、原則 `SpreadsheetApp` の範囲で実装する。
